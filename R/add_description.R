
add_description <- function (stat_units, label) {
  n <- length(stat_units)
  hash <- get_hash(n) # wrong model, hash should be linked with stat_unit
  # TODO: create a function to get hash from stat_unit stat_unit_from_hash ?
}
